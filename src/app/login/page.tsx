'use client'
import { useState } from 'react'
export default function Login(){ const [email,setEmail]=useState('admin@company.com'); const [password,setPassword]=useState('admin123'); async function doLogin(){ const r=await fetch('/api/login',{method:'POST',body:JSON.stringify({email,password})}); if(!r.ok) return alert('Invalid'); const d=await r.json(); document.cookie=`session_token=${d.token}; Path=/;`; location.href='/admin' } return (<div style={{maxWidth:420,margin:'80px auto',padding:16}}><h1>Login</h1><input value={email} onChange={e=>setEmail(e.target.value)} placeholder='Email' style={{display:'block',width:'100%',margin:'8px 0'}}/><input type='password' value={password} onChange={e=>setPassword(e.target.value)} placeholder='Password' style={{display:'block',width:'100%',margin:'8px 0'}}/><button onClick={doLogin}>Login</button></div>) }
